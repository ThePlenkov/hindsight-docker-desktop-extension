services:
  api:
    image: ${DESKTOP_PLUGIN_IMAGE}
    volumes:
      - agent-memory-config:/data

  hindsight:
    image: ghcr.io/vectorize-io/hindsight:latest
    depends_on:
      - api
    entrypoint: ["/bin/sh", "-c"]
    command:
      - |
        [ -f /config/hindsight.env ] && . /config/hindsight.env
        exec /app/start-all.sh
    ports:
      - "8888:8888"
      - "9999:9999"
    environment:
      - HINDSIGHT_API_LLM_PROVIDER=${LLM_PROVIDER:-mock}
      - HINDSIGHT_API_LLM_MODEL=${LLM_MODEL:-}
      - HINDSIGHT_API_LLM_API_KEY=${LLM_API_KEY:-}
      - HINDSIGHT_API_LLM_BASE_URL=${LLM_BASE_URL:-}
      - HINDSIGHT_API_LLM_MAX_CONCURRENT=${LLM_MAX_CONCURRENT:-1}
      - HINDSIGHT_API_ENABLE_OBSERVATIONS=${ENABLE_OBSERVATIONS:-false}
      - HINDSIGHT_API_SKIP_LLM_VERIFICATION=true
      - HINDSIGHT_API_LAZY_RERANKER=true
    volumes:
      - hindsight-data:/home/hindsight/.pg0
      - agent-memory-config:/config:ro

volumes:
  hindsight-data:
    name: agent-memory-hindsight-data
    external: true
  agent-memory-config:
    name: agent-memory-config
    external: true
